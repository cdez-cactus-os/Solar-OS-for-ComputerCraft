term.clear()
term.setCursorPos(1,1)

print("Press any key")
while true do
	event, param = os.pullEvent("key")
	if event == "key" then
		print("Keycode is '" .. param .. "'")
		break
	end
	sleep(0.5)
end
