if fs.exists("eject") then
	fs.delete("eject")
end
if fs.exists("install") then
	fs.delete("install")
end
local disks = {peripheral.find("drive")}
for a = 1, #disks do
	disk.eject(disks[a])
end
os.reboot()
